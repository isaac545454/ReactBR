import { use{{name}} } from './hook'
import { {{ name }}Props } from './types'

export const {{ name }} = ({ }: {{ name }}Props) => {
  const { } = use{{ name }}()
  
  return (
    <div></div>
  );
}

 
